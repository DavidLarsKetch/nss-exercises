"use strict";

const { assert: { } } = require('chai');

const { listChildsToy } = require('../lib/listChildsToy');

describe('listChildsToy module', () => {
  describe('listChildsToy()', () => {
  });
});
